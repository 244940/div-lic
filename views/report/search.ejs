<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Tester Results</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

<h1>Search Tester Results</h1>

<form action="/api/search" method="GET">
    <!-- Search by Name -->
    <label for="search_query">Enter Name:</label><br/>
    <!-- You can also add a dropdown to search by first or last name -->
    <input type="text" id="search_query" name="search_query" required placeholder="First or Last Name">

    <!-- Search Button -->
    <button type="submit">Search</button><br/><br/>
</form>

<% if (results && results.length > 0) { %> 
<h2>Search Results:</h2> 
<ul> 
<% results.forEach(result => { %> 
<li><a href="/results/<%= result.id %>"><%= result.first_name %> <% result.last_name %></a></li> 
<% }); %> 
</ul> 
<% } else if (results) { %> 
<p>No results found.</p> 
<% } %>

<a href="/">Back to Form</a>

</body> 
</html> 